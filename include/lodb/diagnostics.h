#pragma once

/**
 * Optional LoDB diagnostics harness (see `lodb_diagnostics()` in `diagnostics.cpp`).
 * Uses shipped `lodb.DiagnosticsTest` nanopb types in `<lodb/diagnostics.pb.h>`.
 */
void lodb_diagnostics();
